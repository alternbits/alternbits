<!doctype html>
<html lang="en">
<head>
  <title>Submit an AI · {{ appName }} for Makers</title>
  {{ template "_slash_head.tmpl" . }}
</head>
<body x-data="{
  darkMode: document.documentElement.classList.contains('dark'),
  toggle() {
    this.darkMode = !this.darkMode;
    this.darkMode
      ? document.documentElement.classList.add('dark')
      : document.documentElement.classList.remove('dark');
    localStorage.setItem('theme', this.darkMode ? 'dark' : 'light');
  }
}" class="min-h-screen bg-white dark:bg-[#37353E] text-zinc-900 dark:text-white antialiased">

  {{ template "_header.tmpl" . }}
  {{ template "_sidebar.tmpl" . }}

  <main class="mx-auto max-w-xl px-4 py-16">

    {{ if .Submitted }}
    <div class="rounded-2xl border border-green-200 bg-green-50 dark:border-green-800 dark:bg-green-950/40 px-6 py-8 text-center">
      <div class="mx-auto mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-green-100 dark:bg-green-900">
        <svg class="h-6 w-6 text-green-600 dark:text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
      </div>
      <h2 class="text-xl font-bold text-zinc-900 dark:text-white">Submitted!</h2>
      <p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">Your AI is pending review. We'll add it to the directory once approved.</p>
      <a href="/maker/submit" class="mt-6 inline-block rounded-lg bg-orange-500 px-4 py-2 text-sm font-semibold text-white hover:bg-orange-600">Submit another</a>
    </div>
    {{ else }}

    <h1 class="text-3xl font-black tracking-tight text-zinc-900 dark:text-white">Submit an AI</h1>
    <p class="mt-2 text-sm text-zinc-500 dark:text-zinc-400">Your submission goes into review and will appear on {{ appName }} once approved.</p>

    {{ if .Error }}
    <div class="mt-4 rounded-lg border border-red-200 bg-red-50 dark:border-red-800 dark:bg-red-950/40 px-4 py-3 text-sm text-red-700 dark:text-red-400">{{ .Error }}</div>
    {{ end }}

    <form method="POST" action="/maker/submit" class="mt-8 space-y-5">

      <div>
        <label for="name" class="block text-sm font-medium text-zinc-700 dark:text-white">Name <span class="text-red-500">*</span></label>
        <input type="text" id="name" name="name" required value="{{ .Form.Name }}"
               placeholder="e.g. ChatGPT"
               class="mt-1 block w-full rounded-lg border border-zinc-300 dark:border-[#4A4854] bg-white dark:bg-[#3F3D48] px-3 py-2 text-sm text-zinc-900 dark:text-white placeholder-zinc-400 dark:placeholder-zinc-500 focus:border-orange-500 focus:outline-none focus:ring-1 focus:ring-orange-500">
      </div>

      <div>
        <label for="website" class="block text-sm font-medium text-zinc-700 dark:text-white">Website</label>
        <input type="url" id="website" name="website" value="{{ .Form.Website }}"
               placeholder="https://..."
               class="mt-1 block w-full rounded-lg border border-zinc-300 dark:border-[#4A4854] bg-white dark:bg-[#3F3D48] px-3 py-2 text-sm text-zinc-900 dark:text-white placeholder-zinc-400 dark:placeholder-zinc-500 focus:border-orange-500 focus:outline-none focus:ring-1 focus:ring-orange-500">
      </div>

      <div>
        <label for="subtitle" class="block text-sm font-medium text-zinc-700 dark:text-white">Short description</label>
        <input type="text" id="subtitle" name="subtitle" value="{{ .Form.Subtitle }}"
               placeholder="One-line summary"
               class="mt-1 block w-full rounded-lg border border-zinc-300 dark:border-[#4A4854] bg-white dark:bg-[#3F3D48] px-3 py-2 text-sm text-zinc-900 dark:text-white placeholder-zinc-400 dark:placeholder-zinc-500 focus:border-orange-500 focus:outline-none focus:ring-1 focus:ring-orange-500">
      </div>

      <div>
        <label for="description" class="block text-sm font-medium text-zinc-700 dark:text-white">Description</label>
        <textarea id="description" name="description" rows="4"
                  placeholder="Tell us more about this AI…"
                  class="mt-1 block w-full rounded-lg border border-zinc-300 dark:border-[#4A4854] bg-white dark:bg-[#3F3D48] px-3 py-2 text-sm text-zinc-900 dark:text-white placeholder-zinc-400 dark:placeholder-zinc-500 focus:border-orange-500 focus:outline-none focus:ring-1 focus:ring-orange-500">{{ .Form.Description }}</textarea>
      </div>

      <button type="submit"
              class="w-full rounded-lg bg-orange-500 px-4 py-2.5 text-sm font-semibold text-white hover:bg-orange-600 focus:outline-none focus:ring-2 focus:ring-orange-500 focus:ring-offset-2">
        Submit for review
      </button>

    </form>
    {{ end }}

  </main>

  {{ template "_footer.tmpl" . }}
</body>
</html>
